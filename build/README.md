# Build Surface (Wave 1)

This directory is reserved for packaging outputs and release helper artifacts.

Current wave includes:
1. `build/bin/` output target for local executable builds.
2. `scripts/build.ps1` for direct Go builds.
3. `build/package.ps1` placeholder for later packaging automation.

No installer or signing workflow is implemented in wave 1.
